[project]
name = "algo_trade"
version = "0.1.0"
dependencies = ["ibapi", "pydantic", "pandas"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
